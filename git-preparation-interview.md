# Q1: Version control system?
Version control is a system which it will track the evry changes made to the file its allow to maintain the history of changes, collaborate with multiple developers and revert to the previous version whenever is requried.
---
# Q2: Why do companies use Git?
Companies use Git because multiple developers work on the same project. Git helps us track every change made to the code and maintain the complete version history. We can identify who made the changes and when they were made. If something goes wrong, we can easily roll back to a previous version. Git also makes collaboration easier by allowing developers to work on different branches and merge their code safely
---
# Q3: What will happen if a company does not use Git?
If a company does not use Git, multiple developers working on the same project may overwrite each other's code. It becomes difficult to track who made the changes and when they were made. There is no proper version history, so rolling back to a previous version is difficult. Collaboration between developers becomes harder, and managing the source code becomes messy. Git solves all these problems by providing version control and a central repository.
